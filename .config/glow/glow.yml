# style name or JSON path (default "auto")
style: "auto"
# Enable pager: THIS DOES NOT WORK
# pager: ${PAGER:/usr/bin/less}
# show local files only; no network (TUI-mode only)
# local: true
# word-wrap at width
width: 80
